apiVersion: v1
kind: Secret
metadata:
  name: github-token
  namespace: opencode-analyzer
type: Opaque
stringData:
  # Your GitHub Personal Access Token
  # Must have permissions: repo, read:user, write:discussion
  # Create at: https://github.com/settings/tokens
  GITHUB_TOKEN: "YOUR_GITHUB_TOKEN_HERE"
