apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: opencode-output
  namespace: opencode-analyzer
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  # If you have a specific StorageClass, uncomment and set:
  # storageClassName: standard
